import React from 'react';


const Events = (props) => {
  return (
    <div>
      <div>SOME STUFF</div>
    </div>
  )
}

Events.propTypes = {
  SOMEDATA: React.PropTypes.object.isRequired
};

export default Events;